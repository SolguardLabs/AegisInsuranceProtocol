name: CI

on:
  push:
  pull_request:

jobs:
  python-vyper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
      - run: bash scripts/ci.sh
